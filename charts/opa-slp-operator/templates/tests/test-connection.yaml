apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opa-slp-operator.fullname" . }}-test-connection"
  labels:
    {{- include "opa-slp-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opa-slp-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
